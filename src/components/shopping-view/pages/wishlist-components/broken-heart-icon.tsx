import React from "react";
import { AiOutlineHeart } from "react-icons/ai";

const BrokenHeartIcon: React.FC<{ className?: string }> = ({ className }) => {
  return <AiOutlineHeart className={className} />;
};

export default BrokenHeartIcon;
